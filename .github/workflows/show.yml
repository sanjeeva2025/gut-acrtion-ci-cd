  name: Example Workflow

  on: [push]

  jobs:
    build:
      runs-on: ubuntu-latest

      steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Actions secrets and variables
        run: echo "MY_SECRET_KEY=${{ secrets.FIRST_NAME }}" >> $GITHUB_ENV